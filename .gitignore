# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
MANIFEST

# Virtual Environment
venv/
env/
ENV/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints

# Raw datasets -- not committed (size + redistribution license concerns).
# Re-download from the sources in README.md and place directly in our_work/
# before running the notebook.
our_work/*.csv
our_work/*.xlsx

# Generated/regenerable artifacts from running the notebook -- reproducible
# from source + the raw datasets above, not meant to be committed.
our_work/*.npy
our_work/*.pt
our_work/*.pth
our_work/*.png

# Original scaffold project (src/, tests/, CI, tooling configs, the paper
# authors' raw notebook) -- intentionally not part of this repo going
# forward. Recoverable from git history (see README.md) if ever needed.
.agents/
.claude/
tests/
FL-DP-WUSTL.ipynb
requirements.txt
pytest.ini
.flake8
.isortrc
.pre-commit-config.yaml
